export enum ConversationType {
  DIRECT = "direct",
  GROUP = "group",
}

export enum MessageStatus {
  DELIVERED = "delivered",
  FAILED = "failed",
  READ = "read",
  SENT = "sent",
}

export enum Theme {
  DARK = "dark",
  LIGHT = "light",
  SYSTEM = "system",
}
